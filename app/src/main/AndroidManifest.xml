<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LabelIQ">

        <activity
            android:name=".presentation.activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.activities.ScanActivity"
            android:exported="false"
            android:label="@string/title_scan" />

        <activity
            android:name=".presentation.activities.ResultActivity"
            android:exported="false"
            android:label="Analysis Result" />

        <activity
            android:name=".presentation.activities.HistoryActivity"
            android:exported="false"
            android:label="Scan History" />

        <activity
            android:name=".presentation.activities.ResultDetailActivity"
            android:exported="false"
            android:label="Scan Detail" />

        <activity
            android:name=".presentation.activities.SetupActivity"
            android:exported="false"
            android:label="Setup" />

        <activity
            android:name=".presentation.activities.ProfileActivity"
            android:exported="false"
            android:label="My Profile" />

    </application>

</manifest>
